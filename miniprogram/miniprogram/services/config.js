/**
 * API 配置
 */
const config = {
  // 临时域名（备案期间使用 cpolar）
  baseUrl: 'https://765c95c6.r11.vip.cpolar.cn',
  
  // API 版本前缀
  apiPrefix: '/api/v1',
  
  // 请求超时时间 (ms)
  timeout: 30000,
  
  // 默认搜索限制
  defaultSearchLimit: 20,
};

module.exports = config;




