/**
 * API 配置
 */
const config = {
  // 开发环境 API 地址
  baseUrl: 'http://localhost:8000',
  
  // API 版本前缀
  apiPrefix: '/api/v1',
  
  // 请求超时时间 (ms)
  timeout: 30000,
  
  // 默认搜索限制
  defaultSearchLimit: 20,
};

module.exports = config;




