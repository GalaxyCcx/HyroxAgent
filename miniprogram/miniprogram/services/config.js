/**
 * API 配置
 */
const config = {
  // 临时域名（备案期间使用 cpolar）
  baseUrl: 'https://www.hyroxagent.cn',
  
  // API 版本前缀
  apiPrefix: '/api/v1',
  
  // 请求超时时间 (ms)
  timeout: 30000,
  
  // 默认搜索限制
  defaultSearchLimit: 20,
};

module.exports = config;




